services:
  web-angular:
    image: ecommerce-angular
    container_name: ecommerce-angular
    restart: always
    environment:
      API_URL: /api
    ports:
      - "4200:80"
    networks:
      - ecommerce-net

networks:
  ecommerce-net:
    driver: bridge
